/**
 * Platform globals entry for the Chromium build.
 *
 * The Vite alias `dsm-platform-globals` resolves to this file by default, so
 * the import in src/content/index.js is a no-op on extension targets.
 */
